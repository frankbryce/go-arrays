QUESTIONS:
1) when you instantiate a slice (say slice2) from another slice (say slice1),
   does slice2 change when you change slice1?
   ans) yes
2) when you define a definition of a constant list, does the same expression
   apply to all items in the list?
   ans) yes
   a) what about if that expression includes iota?
      ans) yes
3) how do you install a remote package?
   ans) many of the packages are importable right out of the gate. But for
        packages which are not, simply create a sub-directory in the project
        you are making and put the package in there.  Otherwise, there should
        be a way to have a centralized package location.
4) how do you run a test?
   ans) 